import { Popconfirm } from 'antd';
import React from 'react';
export default function ProfileModal () {
    // const {isOpen} = props;
    return (
        <Popconfirm
            // isOpen={isOpen}
            placement="topRight"
            title="ahihi"
            okText="Yes"
            cancelText="No"
      >
      </Popconfirm>
    );
}
